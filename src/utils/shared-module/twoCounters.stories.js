// TODO: Implement the counters with actions to save and retrieve values from the shared store.
storiesOf('Shared Module', module)
   .add('Two counters', () => {
      return <p>Here are two counters that increment value in a shared store.</p>
   });